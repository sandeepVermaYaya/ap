const {env}= require("./environment")
const {enum_data, status}=require("../constents/common")
const {routes}=require("../constents/routes")
module.exports={
    env,
    enum_data,
    routes,
    status
}
const {env}= require("./environment")
const {enum_data, status, dashboard_status, dashboard_type}=require("../constents/common")
const {routes}=require("../constents/routes")
module.exports={
    env,
    enum_data,
    routes,
    status,
    dashboard_status,
    dashboard_type
}